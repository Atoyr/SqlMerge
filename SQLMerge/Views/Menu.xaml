<UserControl x:Class="SQLMerge.Views.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLMerge.Views"
             mc:Ignorable="d" 
             Height="{x:Static SystemParameters.MenuBarHeight}"
             d:DesignHeight="{x:Static SystemParameters.MenuBarHeight}" d:DesignWidth="300">
    <Menu VerticalAlignment="Center">
        <!--<MenuItem Header="ファイル(_F)" ItemContainerStyle="{DynamicResource MenuItemStyle1}" Template="{DynamicResource MenuItemControlTemplate2}" >
            <MenuItem IsCheckable="False" Header="オプション2" />
            <MenuItem IsCheckable="False" Header="オプション3" />
        </MenuItem>-->
        <MenuItem IsCheckable="False" Header="オプション1">
            <MenuItem IsCheckable="True" Header="オプション1" />
            <MenuItem IsCheckable="True" Header="オプション2" />
        </MenuItem>
        <MenuItem Header="編集(_E)" />
        <MenuItem Header="オプション(_O)">
            <MenuItem IsCheckable="True" Header="オプション1" />
            <MenuItem IsCheckable="True" Header="オプション2" />
            <MenuItem IsCheckable="True" Header="オプション3" />
        </MenuItem>
    </Menu>
</UserControl>
